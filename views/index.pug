extends layout

block contents
  
  form#user-form
    fieldset
      legend 로그인
      div
        input#username(type="text" placeholder="아이디를 입력하세요")
      div
        input#userpsw(type="password" placeholder="비밀번호를 입력하세요")
      div
        input#issave(type="checkbox")
        label(for="saveID") ID 저장
      button(type="submit") 로그인


  script(serc='https://unpkg.com/axios/dist/axios.min.js')
  script(src='/sequelize.js')